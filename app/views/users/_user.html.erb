<p>
<h2>My profile:</h2>
<strong>My Email:</strong>
<p>
  <%= user.email %>
</p>

<h3>Articles I Wrote:</h3>

<!--If articles are empty, put a message-->

<% if not user.articles.empty? %>
  <% user.articles.each do |article| %>
    <ul>
      <li>
        <%= link_to article.title, article %>
      </li>
    </ul>
  <% end %>

<% else %>
  <!-- Message here -->
  Sorry, no articles!
<% end %>

<!--If articles are empty, put a message-->
<h3>Comments I Wrote:</h3>

<% if not user.comments.empty? %>
  <% user.comments.each do |comment| %>
    <ul>
      <li>
        <%=
          comment.message
        %>
        on <%= link_to comment.article.title, comment.article %>
      </li>
    </ul>
  <% end %>

<% else %>
  <!-- Message here -->
  Sorry, no comments!
<% end %>