<p>
<h2>My profile:</h2>
<strong>My Email:</strong>
<p>
  <%= user.email %>
</p>

<h3>Articles I Wrote:</h3>

<!--If articles are empty, put a message-->

<% if not user.articles.empty? %>
  <% user.articles.each do |article| %>
    <%= link_to article.title, article %>
  <% end %>

<% else %>
  <!-- Message here -->
  Sorry, no articles!
<% end %>

<!--If articles are empty, put a message-->
<h3>Comments I Wrote:</h3>

<% if not user.comments.empty? %>
  <% user.comments.each do |comment| %>
    <%= link_to comment.article.title, comment.article.title %>
  <% end %>

<% else %>
  <!-- Message here -->
  Sorry, no comments!
<% end %>